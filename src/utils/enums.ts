export enum Environment {
  Dev = 'development',
  Prod = 'production',
  Test = 'test',
}

export enum LanguageFile {
  Country = 'country.json',
  Category = 'category.json',
}

export enum Indices {
  Product = 'product',
}
